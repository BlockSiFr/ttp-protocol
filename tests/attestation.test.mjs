import test from 'node:test';import assert from 'node:assert/strict';import {verify_attestation} from '../src/index.mjs';
const att={ref:'att-1',subject:'agent:invoice-bot',issuer:'issuer:enterprise-idp',type:'workload',issuedAt:'2026-04-29T13:00:00Z',expiresAt:'2026-04-29T15:00:00Z',claims:{mfa:true},trustScoreDelta:0.1};
const base={attestation:att,subject:'agent:invoice-bot',requiredType:'workload',requiredIssuer:'issuer:enterprise-idp',validAt:'2026-04-29T14:00:00Z',maxAge:7200,requiredClaims:['mfa']};
test('valid',()=>assert.equal(verify_attestation(base).valid,true));test('wrong subject',()=>assert.equal(verify_attestation({...base,subject:'x'}).valid,false));test('wrong issuer',()=>assert.equal(verify_attestation({...base,requiredIssuer:'issuer:x'}).valid,false));test('expired',()=>assert.equal(verify_attestation({...base,validAt:'2026-04-29T16:00:00Z'}).valid,false));test('stale',()=>assert.equal(verify_attestation({...base,maxAge:100}).valid,false));test('missing claim',()=>assert.equal(verify_attestation({...base,requiredClaims:['missing']}).valid,false));
