import {failure} from './errors.mjs';
export function validate_transfer(i){const r=[];if(i.amount<=0||i.amount>1)r.push(failure('TRANSFER_INVALID','amount invalid'));if(i.currentTrustOfSender<i.amount)r.push(failure('INSUFFICIENT_SENDER_TRUST','sender insufficient'));if(i.minimumReceiverTrust!=null&&(i.receiverTrust??0)<i.minimumReceiverTrust)r.push(failure('TRANSFER_INVALID','receiver below minimum'));if(new Date(i.expiresAt)<new Date(i.currentTime))r.push(failure('TRANSFER_INVALID','expired'));if((i.conditions||[]).some(c=>!c.satisfied))r.push(failure('TRANSFER_INVALID','conditions unmet'));return {valid:r.length===0,from:i.from,to:i.to,amount:i.amount,dimension:i.dimension,expiresAt:i.expiresAt,revocable:i.revocable,transferable:i.transferable,senderDelta:-i.amount,receiverDelta:i.amount,failureReasons:r};}
