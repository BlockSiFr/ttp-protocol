import {failure} from './errors.mjs';
export function verify_delegation(i){const r=[];if(i.delegator===i.delegatee&&!i.allowSelf)r.push(failure('INVALID_INPUT','self delegation disallowed'));if(new Date(i.expiresAt)<new Date(i.currentTime))r.push(failure('DELEGATION_EXPIRED','expired'));if(!i.scope.includes(i.requestedAction))r.push(failure('DELEGATION_SCOPE_VIOLATION','action out of scope'));if(i.amount<0||i.amount>1)r.push(failure('TRANSFER_INVALID','amount invalid'));if((i.proofRefs||[]).some(p=>!p))r.push(failure('INVALID_INPUT','invalid proof ref'));if(i.transitiveRequest&&!i.transferable)r.push(failure('DELEGATION_SCOPE_VIOLATION','transitive not allowed'));return {valid:r.length===0,delegator:i.delegator,delegatee:i.delegatee,scope:i.scope,amount:i.amount,transferable:i.transferable,expiresAt:i.expiresAt,failureReasons:r};}
