# Route Proof Profile
A route is valid only if all hops are valid, issuers are trusted, policy allows domain/action/resource, hop count is within maxHops, and intermediate trust remains above threshold after hop decay.

Route hash binds route evidence to proof and downstream receipt.
