# MCP Trust Profile
MCP exposes tools; TTP proves actor/tool route trustworthiness.
SCIM-RE normalizes invocation authority; RAP returns execution decision.

Include tool argument hash and deny behavior when trust proof is invalid.
