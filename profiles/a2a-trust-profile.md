# A2A Trust Profile
A2A transports tasks/messages; TTP proves trust state behind those tasks.
TRP resolves route; SCIM-RE/RAP enforce decisions.

Example agent card extension includes `x-ttp` trust metadata and authority endpoint.
